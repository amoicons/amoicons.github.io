{
  "name": "iconsite",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "react": "^15.5.4",
    "react-dom": "^15.5.4"
  },
  "homepage": "https://amoicons.github.io/site",
  "devDependencies": {
    "amoicons": "^0.0.6",
    "gh-pages": "^0.12.0",
    "react-scripts": "0.9.5",
    "svg-sprite-loader": "^0.3.0"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  }
}
